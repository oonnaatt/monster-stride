// Auth is handled by Supabase client-side; this file is a placeholder for future auth routes
import type { FastifyInstance } from 'fastify';

export async function authRoutes(_fastify: FastifyInstance) {
  // Auth managed by Supabase client SDK
}
